using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Managers;

public class ExperimentalCollectable : MonoBehaviour {
    //XRCardboardController cardboardController;

    //// Start is called before the first frame update
    //void Start() {
    //    cardboardController = GetComponentInParent<XRCardboardController>();
    //}

    //// Update is called once per frame
    //private void Update() {
    //    if (cardboardController.IsObjectDetected()
    //        && cardboardController.IsTriggerPressed()) {
    //        Destroy(this.gameObject);
    //    }
    //}


    
}